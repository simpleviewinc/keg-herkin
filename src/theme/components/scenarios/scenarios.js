import { scenario } from './scenario'
import { addScenario } from './addScenario'

export const scenarios = {
  main: {
    flex: 1,
  },
  scenario,
  addRow: {},
  scenariosRow: {
    flex: 1,
  },
  add: addScenario,
}
