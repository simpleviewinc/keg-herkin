import React from 'react'
// import { getRemoteSteps } from 'SVActions/steps'


export const Steps = props => {
  // const { steps } = useSelector(({ items }) => pickKeys(items, [ 'steps' ]), shallowEqual)

  // useEffect(() => {
  //   getRemoteSteps({ type: 'steps' })
  // }, [])

  
  return (
    <div>
      Steps
    </div>
  )
}