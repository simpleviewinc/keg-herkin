import { tapColors } from '../tapColors'

export const runner = theme => {
  return {
    main: {
      flexDirection: 'row',
      width: `100%`,
      height: `100%`,
      bgC: theme.colors.palette.white01,
    },
  }
}
