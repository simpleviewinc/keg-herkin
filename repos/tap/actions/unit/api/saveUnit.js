

/**
 * Saves a unit tests fileModel to the local file system by calling the backend API
 * @type function
 * @param {Object} unit tests - FileModel representing the unit tests
 *
 * @returns {void}
 */
export const saveUnit = (unit) => {
  // TODO
}